# Continuity / working files — local only, never commit (per global CLAUDE.md)
HANDOFF.md
TASKS.todo
CHANGELOG.md

# Design exploration / preview artifacts — local only
assets/*.html
assets/gen_*.py
# EXCEPTION: the header generator runs in CI, so it must be committed
!assets/gen_header.py
# superseded bespoke-banner concepts
assets/header-A*
# intermediate SVGs (the chosen masthead PNG + header.png are committed explicitly)
assets/masthead-*.svg
assets/header.svg

# Local design scratch (never commit)
.firecrawl/
.playwright-mcp/
jjv-context-*.png

# OS cruft
.DS_Store
